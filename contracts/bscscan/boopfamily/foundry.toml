[profile.default]
src = "src"
out = "out"
libs = ["lib"]
cache_path = "cache"
solc_version = "0.8.28"
optimizer = true
optimizer_runs = 800
via_ir = true
evm_version = "cancun"

remappings = [
  "@openzeppelin/=lib/openzeppelin-contracts/",
]